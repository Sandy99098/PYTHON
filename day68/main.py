# Your Python code goes here
# Excercise 7
# write the program to clar the clutter inside a folder on your computer
# you should use os model to rename all the png images form 1.png all 
# the way tull n.png where n is the number of png  in that folder .
# Do the same for other file formats
# import os


 

# import os

# # Define the directory containing the files
# directory = "folder"

# # Iterate over the files in the directory
# for i in range(5):
#     old_name = os.path.join(directory, f"folder{i}.png")  # Construct the old file path
#     new_name = os.path.join(directory, f"{i}.png")  # Construct the new file path
#     os.rename(old_name, new_name)  # Rename the file
